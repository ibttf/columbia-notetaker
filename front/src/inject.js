import React from "react"
import ReactDOM from "react-dom"
import MiniBanner from "./components/MiniBanner" // Adjust the path if necessary based on your app structure

const mountNode = document.getElementById("vidnotes-extension-root")
ReactDOM.render(<MiniBanner />, mountNode)
